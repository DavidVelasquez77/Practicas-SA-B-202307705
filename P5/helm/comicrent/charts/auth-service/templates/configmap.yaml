apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "auth-service.fullname" . }}-config
  namespace: {{ .Values.global.namespace | default "sa-p5" | quote }}
  labels:
    {{- include "auth-service.labels" . | nindent 4 }}
data:
  PORT: {{ .Values.config.port | default "3001" | quote }}