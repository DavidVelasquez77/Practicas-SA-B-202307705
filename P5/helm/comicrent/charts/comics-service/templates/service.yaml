apiVersion: v1
kind: Service
metadata:
  name: {{ include "comics-service.fullname" . }}
  labels:
    {{- include "comics-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "comics-service.selectorLabels" . | nindent 4 }}
