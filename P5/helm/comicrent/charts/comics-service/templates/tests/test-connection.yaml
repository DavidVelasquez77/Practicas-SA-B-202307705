apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "comics-service.fullname" . }}-test-connection"
  labels:
    {{- include "comics-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "comics-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
