apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "copies-service.fullname" . }}-test-connection"
  labels:
    {{- include "copies-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "copies-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
