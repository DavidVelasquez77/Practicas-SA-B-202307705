apiVersion: v1
kind: Service
metadata:
  name: {{ include "copies-service.fullname" . }}
  labels:
    {{- include "copies-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "copies-service.selectorLabels" . | nindent 4 }}
