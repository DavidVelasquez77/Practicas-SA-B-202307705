apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rentals-service.fullname" . }}-test-connection"
  labels:
    {{- include "rentals-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rentals-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
