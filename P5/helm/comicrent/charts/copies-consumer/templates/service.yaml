apiVersion: v1
kind: Service
metadata:
  name: {{ include "copies-consumer.fullname" . }}
  labels:
    {{- include "copies-consumer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "copies-consumer.selectorLabels" . | nindent 4 }}
