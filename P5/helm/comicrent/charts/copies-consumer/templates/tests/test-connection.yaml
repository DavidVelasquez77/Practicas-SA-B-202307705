apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "copies-consumer.fullname" . }}-test-connection"
  labels:
    {{- include "copies-consumer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "copies-consumer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
